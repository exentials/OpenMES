@namespace OpenMES.WebClient.Components.Actions
@using Microsoft.FluentUI.AspNetCore.Components

@* QualityReadingScreen - Quality Inspection Reading Screen
    Phase 8 (WEB-020)
    Allows operator to record quality measurements and readings
    
    State Machine: Main → QualityReadingScreen → [input readings] → [validate] → API → Main
*@

<FluentStack Orientation="Orientation.Vertical" VerticalGap="20">
    <FluentCard>
        <h3>Quality Readings</h3>
        <FluentLabel>Feature coming in Phase 8 (WEB-020)</FluentLabel>
    </FluentCard>
</FluentStack>

@code {
    // Placeholder - to be implemented in Phase 8
}
